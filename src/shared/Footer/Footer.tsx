import Logo from '../Logo/Logo';
import './Footer.scss';

function Footer(): JSX.Element {
    return(
        <footer className='footer'>
            <Logo/>
        </footer>
    );
}

export default Footer;